import React from "react";
import { View, Text } from "react-native";

const Title: React.FC = () => {
    return (
      <View>
        <Text>TEXT2</Text>
      </View>
    );
  };

export default Title;